// the order of tab in this enum must be the same as in the filtering-tabs.component template
export enum ActiveTab {
    allOrders,
    toValidate,
    toImport,
    importErrors,
    toShip,
    shippingErrors,
    shipped,
}
