import { MinNumberDirective } from './min-number.directive';

describe('MinNumberDirective', () => {
    it('should create', () => {
        const directive = new MinNumberDirective(<any>{});
        expect(directive).toBeTruthy();
    });
});
