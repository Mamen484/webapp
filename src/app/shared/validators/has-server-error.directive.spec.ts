import { HasServerErrorDirective } from './has-server-error.directive';

describe('HasServerErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new HasServerErrorDirective();
    expect(directive).toBeTruthy();
  });
});
