import { SfuiDropdownTriggerDirective } from './dropdown-trigger.directive';

describe('DropdownTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new SfuiDropdownTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
