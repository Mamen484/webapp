import { SearchStoreDirective } from './search-store.directive';

describe('SearchStoreDirective', () => {
    it('should create an instance', () => {
        const directive = new SearchStoreDirective();
        expect(directive).toBeTruthy();
    });
});
