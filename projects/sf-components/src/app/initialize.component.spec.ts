import { InitializeDirective } from './initialize.component';

describe('InitializeDirective', () => {
  it('should create an instance', () => {
    const directive = new InitializeDirective();
    expect(directive).toBeTruthy();
  });
});
